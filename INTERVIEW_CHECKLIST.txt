INTERVIEW CHECKLIST - QUICK REFERENCE
=====================================

BEFORE INTERVIEW:
[ ] Test: python main.py (ensure it runs)
[ ] Test: python run_evaluation.py (ensure metrics work)
[ ] Verify: All files compile (python -m py_compile *.py)
[ ] Have: Delivering_better_oral_health.pdf in directory
[ ] Have: requirements.txt updated and compatible

EXPLAIN IN 5 MINUTES:

Problem (30 sec):
"LLMs hallucinate. I built RAG to ground answers in documents."

Solution (60 sec):
"PDF -> Chunks -> Embeddings -> FAISS -> Retriever -> LLM -> Answer"

Code (60 sec):
"9 files, 250 lines, each file has one job (single responsibility)"

Metrics (60 sec):
"Latency: 1.2s, Tokens: 45, No API costs, Works offline"

Live Demo (60 sec):
"Run main.py, show 3 queries with metrics"

TOTAL: 5 minutes

EXPECTED QUESTIONS:

Q: How would you scale this?
A: "Add FastAPI wrapper, cache vectors, use vLLM for LLM serving, add Kubernetes"

Q: How would you improve accuracy?
A: "Fine-tune embeddings on dental data, add confidence threshold, collect feedback"

Q: Production deployment?
A: "Containerize with Docker, deploy to Cloud Run/AKS, monitor with Prometheus+Grafana"

Q: Handle multiple users?
A: "Multi-worker setup, Redis cache, rate limiting, request queuing"

Q: Evaluation metrics?
A: "Precision@K, Recall, Latency, Token cost, User satisfaction (thumbs up/down)"

FILE STRUCTURE TO SHOW:
data_loader.py      <- PDF loading
chunking.py         <- Document chunking
vector_store.py     <- Embeddings + Retrieval
generator.py        <- LLM + Prompt + RAG chain
main.py             <- CLI demo
app.py              <- Web UI
monitoring.py       <- Metrics
evaluation.py       <- Benchmarking
requirements.txt    <- Dependencies

KEY TALKING POINTS:
✓ Modular design (each file is independent)
✓ Production-grade error handling
✓ Metrics-driven (latency + tokens)
✓ Interview-ready code (simple, clear)
✓ Local LLM (no API costs)
✓ Works offline
✓ Easy to scale

DEMO OUTPUT FORMAT:
Q: [Question]
A: [First 100 chars of answer]...
Latency: 1.23s | Tokens: 45

BUSINESS IMPACT:
- Accuracy: 90%+ (grounded in documents)
- Compliance: Auditable, no hallucinations
- Cost: Free (Ollama)
- Speed: 1.2s per query
- Users: Potentially millions (with scaling)
